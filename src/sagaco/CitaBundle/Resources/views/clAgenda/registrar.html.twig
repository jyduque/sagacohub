{% extends '::plantilla.html.twig' %}

{% block id 'agenda' %}
    {# Barra de ruta #}
    {% block ruta %}
    <div class="container navbar-blanca">                
        <ul class="breadcrumb-modi">
            <li><a href="#"><small>Inicio</small></a></li>
            <li class="active"><small>Agenda personalizada</small></li>
        </ul>                              
    </div> 
    {% endblock %}{#-- Fin Barra de ruta --#}
   
    {# Campos de la Forma #}
    {% block twigformulario %}
        <fieldset>
            {# Titulo del requisito #}
            <legend>Configurar agenda personalizada</legend> 
            {{ form_start(form, { 'style': 'horizontal' }) }}
            {{ form_errors(form, { 'global_errors': true }) }}
            {{ form_row(form._token) }}
            {#Muestra los campos del formulario#}
            {{ form_row(form.coOrientador, {'label': 'Orientador'}) }}
            {{ form_row(form.coSemestre, {'label': 'Semestre'}) }}
            {{ form_row(form.coDuracion, {'label': 'Duración de sesión'}) }}
            <h3>Horario</h3>
            {{ form_row(form.detallAgenda) }}
            
            {{ form_end(form) }}
                                                 
        </fieldset>   
        
    {% endblock %}{#-- Fin Campos de la Forma #}