{% extends '::plantilla.html.twig' %}

{% block id 'configentrevista' %}
   
    {% block ruta %}
    <div class="container navbar-blanca">                
        <ul class="breadcrumb-modi">
            <li><a href="#"><small>Inicio</small></a></li>
            <li><a href="{{ path('pgConfigEntrevista') }}"><small>Configuración entrevista</small></a></li>
            <li class="active"><small>Crear</small></li>
        </ul>                              
    </div> 
    {% endblock %}{#-- Fin Barra de ruta --#}
    
    {# Campos de la Forma #}
    {% block twigformulario %}
        
            <fieldset>
                {# Titulo del requisito #}
                <legend>Crear configuración entrevista
                    <div class="pull-right">
                        <a href="{{ path('pgConfigEntrevista') }}" class="btn btn-xs" data-toggle="tooltip" data-placement="bottom" title="" data-original-title="Regresar al listado">
                            <span class="glyphicon glyphicon-hand-left" aria-hidden="true"></span>
                        </a>                       
                    </div>
                </legend>
                {#Muestra los campos del formulario#}      
                   
                {{ form_start(form, { 'style': 'horizontal' }) }}
                    {{ form_errors(form, { 'global_errors': true }) }}
                    {{ form_row(form._token) }}
                    {{ form_row(form.coTipoEntrevista, {'label': 'Tipo entrevista'}) }}                    
                    {{ form_row(form.txPregunta, {'label': 'Pregunta'}) }}
                    {{ form_row(form.coTipoPregunta, {'label': 'Tipo pregunta'}) }}                    
                    {{ form_row(form.inActiva, {'label': 'Activa'}) }}
                {{ form_end(form) }}                
            </fieldset>         
            
        
    {% endblock %}{#-- Fin Campos de la Forma #}