{% extends '::base.html.twig' %}
{# Enlace de hojas de estilos CCS #}
{% block hojaestilos %}
    {# Bootstrap #}
    <link href="{{ asset('css/bootstrap.css') }}" rel="stylesheet" media="screen">    
    <link href="{{ asset('css/estilos.css') }}" rel="stylesheet" media="screen">

    {# HTML5 Shim and Respond.js add IE8 support of HTML5 elements and media queries #}
    {% include 'BraincraftedBootstrapBundle::ie8-support.html.twig' %}
{% endblock %}

{# Contenido de la pagina #}
{% block contenido %}
    <header>
        <div>            
            <div class="container">
                <a class="navbar-brand" title="UNET" href="#">
                    <img src="../../../web/images/logo-unet_con_texto_azul2.png" class="img-responsive" alt="UNET"/>
                </a>
                
                {# Usuario conectado #}
                {% block conectado %}{% endblock %}
                    
                
                <h4> {{ sitema | default('Servicio Automatizado de Gestión para la Atención') }}</h4>
                <h4> {{ sistema1 | default('de la Coordinación de Orientación') }}</h4>                
            </div>
                
            {# Menu #}
            {% block menu %}{% endblock %}
            
            <div class="container">
                {# Barra de navegacion #}
                {% block navegacion %}{% endblock %}
            </div>            
        </div>
    </header> 
    <div class="container">
        <!-- Main component for a primary marketing message or call to action -->
        <form class="form-horizontal">
            <fieldset>
                {% block area_trabajo %}
                    {% block titulo_requisito %}
                    {% endblock %} {# -- Fin titulo_requisito #}                
                {% endblock %} {# -- area_trabajo #}
            </fieldset>
        </form>
    </div> <!-- /container -->
{% endblock %}

{# Enlace y contenido de javascripts #}
{% block javascripts %}
    {# jQuery (necessary for Bootstraps JavaScript plugins) #}
    <script src="{{ asset('js/jquery.js') }}"></script>
    
    {# Include all JavaScripts, compiled by Assetic #}
    <script src="{{ asset('js/bootstrap.js') }}"></script>
{% endblock %} 
